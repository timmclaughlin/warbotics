---
// /chat was merged into /search. Redirect to preserve bookmarks and any
// incoming ?q=<query>. The /api/chat/stream endpoint is unchanged.
return Astro.redirect(`/search${Astro.url.search}`, 301);
---
